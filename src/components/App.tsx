import React from 'react';
//import types
import { AppProps } from '../containers/App';

const App = (props: AppProps) => {
  console.log(props);
  return(
    <div>
      app
    </div>
  )
}

export default App